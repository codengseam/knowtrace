# 云脉·智诊伴学 — Python 依赖
# 魔搭 ModelScope Studio 入口（app.py）只需以下依赖即可启动

# 核心依赖
PyYAML>=6.0
python-dotenv>=1.0.0

# Web 服务（魔搭 Studio 入口）
gradio>=4.44.0,<5.0.0

# 阿里云百炼 / Qwen3 / FunASR
dashscope>=1.20.0

# 一键出卷（A4 Word 排版，Phase 2 启用）
python-docx>=1.1.0

# 数据契约 / 校验
pydantic>=2.0

# Markdown → HTML 渲染 + 净化（SSG 章节页构建，复用自 HaloRead BUG-035）
markdown>=3.6
bleach>=6.1

# 开发 / 测试
pytest>=8.0
